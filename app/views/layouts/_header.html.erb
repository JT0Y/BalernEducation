<div class="container">
  <div class="brand">
    <%= link_to image_tag("g.png", height: 175, width: 500), root_path %>
  </div>
  <div class="address-bar">
    <%= t('.address') %>
  </div>
</div>

  <!-- Navigation -->
  <nav class="navbar navbar-default" role="navigation">
    <div class="container">

    
      <div class="navbar-header"> <!-- Brand and toggle grouped for mobile display -->
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <!-- navbar-brand is hidden on larger screens, but visible when the menu is collapsed -->
        <a class="navbar-brand" href="#">Balern Edu.</a>
      </div>
    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li><%= link_to t('.about'), about_path %></li> 
        <li><%= link_to t('.resources'), book_path %></li> 
        <li><%= link_to t('.inquire'), new_inquery_path %></li>

      <li class="dropdown">
      <a href="#" class="dropdown-toggle" data-toggle="dropdown" href="#" aria-expanded="false"><%= t('.account') %><span class="caret"></span>
      </a>
      <ul class="dropdown-menu" role="menu">
        <li><%= link_to t('.messages'), conversations_path %></li>
        <li><%= link_to t('.homework'), "#" %></li>
        <li><%= link_to t('.calendar'), events_path %></li>
        <li><%= link_to t('.photos'), photos_path %></li>
        <li><%= link_to t('.settings'), edit_user_registration_path %></li>
        <% if current_user && current_user.admin? %>
        <li><%= link_to t('.staff'), teachers_path %></li>
        <li><%= link_to t('.students'), students_path %></li>
        <li><%= link_to t('.classes'), groups_path %></li>
        <% end %>

        <li class="divider"></li>
        <!-- START DEVISE Condition !-->
        <% if user_signed_in? %>
          <li><%= link_to t('.logout'), destroy_user_session_path, method: :delete %>        
            <% else %>
          <li><%= link_to t('.signin'), new_user_session_path %></li>
        <% end %>
        <!-- END DEVISE Condition !-->
            </ul>
          </li>
        </ul>
    </div>
  </nav>
  <!-- /.navbar-collapse -->